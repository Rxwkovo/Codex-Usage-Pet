# v2.2.2 安全与健壮性复核

## 结论

本次复核没有发现仍可通过手机同步接口执行命令、读取登录凭据或绕过设备令牌读取额度的已知路径。网络入口继续保持为一次性配对与只读额度查询，并补齐来源限流容量、单来源并发、请求头大小和 Android 配对地址校验。

“没有发现已知漏洞”不等于对任何环境作绝对安全保证。同步功能应只在可信家庭局域网中开启；不用时可在设置页关闭。

## 外部建议评估

### 每 60 秒拉起 PowerShell

这个性能担忧部分成立：当前刷新会启动一个短期 PowerShell 工作者，并由它启动本机 `codex app-server`，进程创建会产生短暂资源峰值。不过读取在后台异步执行，不会阻塞宠物动画，默认间隔也可在设置页调整。

不能直接用 `HttpClient` 替换脚本，因为 `account/rateLimits/read` 是本机 Codex app-server 的 JSON-RPC 方法，并非可直接请求的公开 HTTP API；桌面程序也不应读取或复制 ChatGPT 网页凭据。适合后续版本的优化是让一个受控的本机代理进程长期保持 app-server 会话，出现故障时自动重建连接。

### 内部接口变化

风险成立。现有实现已经在失败时保留最后一次成功数据、标记数据过期并切换为中性未知状态。本版进一步记录连续失败次数；连续三次失败后显示明确气泡与托盘提示，恢复后自动清零。12 秒直连与 25 秒代理重试发生在后台，不会冻结动画。

### 托盘与交互闭环

项目原先已有托盘退出、设置、精确额度和重置时间提示。本版在托盘补充手动刷新、暂停或恢复随机动作、打开 Codex 用量页与切换置顶。透明度、置顶、字体和刷新间隔继续由设置页管理。

## 本版修复

- 来源频率表设定严格容量上限，过期记录清理后仍超限时淘汰最旧记录。
- TLS 握手前限制全局并发为 16、每来源并发为 4。
- 请求行与单个请求头限制为 8 KiB，请求头数量限制为 32。
- Android 只接受显式端口的私有 IPv4 HTTPS 地址，USB 测试仅接受 `127.0.0.1`；拒绝公网地址、域名、路径、查询串和用户信息。
- 配对码、邀请、PIN、设备令牌与证书指纹均有格式和长度上限，使用前重新校验持久化数据。
- GitHub Actions 使用只读权限、关闭凭据持久化，并固定到完整提交哈希；Dependabot 每月检查依赖。

## 验证记录

- Python 同步协议与服务器：30 项自动化测试通过。
- PowerShell：9 组脚本与素材检查通过。
- Android：单元测试、Lint 与 debug APK 构建通过。
- Windows EXE：构建、启动、移动、缩团、唤醒与托盘退出运行检查通过。
- `pip-audit`：`mobile-sync` 与 `bridge` 的锁定依赖未检出已知漏洞。
- Microsoft Defender：最终 EXE 与 APK 未检出威胁。
- PyInstaller 固定为 6.22.2，高于 GHSA-9fxf-4qw3-ghmr 的 6.22.1 修复版本；ZXing 升级为 3.5.4。

这些结果覆盖本仓库代码、锁定依赖和本次生成的安装包，不覆盖操作系统、路由器、Codex 客户端本身或用户主动安装的其他软件。
