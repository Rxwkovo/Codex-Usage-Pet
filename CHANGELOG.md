# 更新日志

## 未发布 — 2026-09-13

### 同步核心

- 将桌面内置同步服务与独立同步桥重复的配对、证书、额度字段过滤和局域网来源限制抽取到同一个 `sync_core.py`；两套入口继续保留各自的生命周期、界面控制和刷新方式。
- 公共核心统一处理一次性邀请、设备令牌哈希、全部撤销、畸形输入、来源频率与并发限制，并继续使用协议 v2 额度白名单，避免两套入口安全规则漂移。
- 两套 PyInstaller 构建显式包含公共核心；新增入口身份、一次性配对、撤销、畸形输入及原有网络安全回归检查。

## v2.2.2 — 2026-09-12

### 安全加固

- 修复来源限流表在大量不同地址同时访问时仍可越过容量上限、持续占用内存的问题。
- TLS 握手前增加每个来源最多 4 条并发连接的限制；单个局域网设备无法再占满全部 16 个工作线程。
- 将请求行和请求头限制为 8 KiB、请求头数量限制为 32，在路由和身份验证前拒绝异常大请求。
- Android 配对仅接受带明确端口的私有局域网 IPv4 或 USB 本机地址；拒绝公网域名、公网 IP、路径、查询串及含用户信息的地址。
- Android 对配对码、解码后内容、证书指纹、邀请和设备令牌设置严格格式及长度上限，并在每次请求前重新验证保存的端点。
- GitHub Actions 改为只读权限，关闭检出凭据持久化，并将全部第三方 Action 固定到经过核对的完整提交哈希。
- Android 二维码解析库升级到 ZXing 3.5.4，包含像素数组输入校验修复。

### 版本与验证

- 桌面版更新为 v2.2.2，自动迁移 v2.2.1 及更早版本设置；Android 更新为 0.2.2-preview（versionCode 5），可覆盖 0.2.1。
- 增加限流容量、单来源并发、超大请求头和 Android 配对端点回归测试；补充安全策略与漏洞报告入口。
- 连续三次额度读取失败后显示明确气泡和失败次数，同时继续保留上次数据并使用中性表情；恢复成功后自动清零。
- 托盘菜单增加手动刷新、暂停／恢复随机动作、查看 Codex 用量页和切换置顶；透明度、置顶、字体和刷新间隔继续由设置页调整。
- 修复待机及走路、坐下、躺下、伸展、挥手的哭泣泪滴被脸部边界裁成半滴的问题；复用原手绘泪滴并按各姿势整体映射，缩团原画保持不变。

## v2.2.1 — 2026-09-12

### 稳定性与安全

- 修复额度读取进程卡住后永久停止刷新；失败时保留上一次读数并明确标为过期，不把缓存冒充实时额度。
- 修复嵌入式与独立同步端可被慢 TLS／慢请求长期占满的问题；加入绝对连接期限、有界并发、每来源频率限制和令牌长度限制。
- 同步端只接受私有局域网 IPv4 与本机测试连接，接口保持为 `POST /pair` 和 `GET /usage`；输出继续使用严格字段白名单。
- 修复证书与私钥并发生成后可能错配、配对与状态文件写入竞争、设备状态无界增长及正常退出被清理异常覆盖的问题。
- 独立同步端的 Windows 防火墙规则增加程序路径限制，与桌面内置同步规则保持一致。

### 桌面与 Android

- 修复手机设置页一次刷新重复读取状态文件的问题，并补上自动开启、启动超时和状态机回归检查。
- 修复 Android 通知图标、缺图崩溃、缩团开关不展开、配对信息失效提示、主线程逐帧解码和拖动频繁写盘等问题。
- 修复 Android 待机四种额度表情被导出成相同图片；保留 520 张 440×420 手绘动作与表情成品帧。
- 同步数据增加不含个人信息的服务端时间，Android 分别在电脑与手机的时钟域内判断新鲜度；两台设备时间有偏差时不再把刚同步的数据误判为过期，并按手机时间显示更新与恢复时刻。
- Android 版本更新为 0.2.1；桌面版升级时自动迁移 v2.2.0 的位置和偏好设置。

### 工程整理与验证

- 将 Android、独立同步桥与 Windows 正式源码合并到 `main`，统一文本文件行尾，并补充 Windows、Python、Android 三套持续集成检查。
- 覆盖 9 组 PowerShell 测试、24 个 Python 同步安全测试、10 个 Android 领域测试、Android Lint、桌面 EXE 构建与启动检查。

## v2.2.0 — 2026-09-12

### 手机连接设置

- 新增设置中的「手机连接」页，整合开启／关闭同步、当前局域网地址、二维码、复制配对码、生成新配对码及解除全部配对。
- 托盘和宠物右键菜单新增「连接手机」快捷入口。关闭设置窗口后同步继续；退出码团后连接服务停止。
- 可设置端口、配对码有效期及随码团启动。首次默认不开启；关闭同步会同时关闭自动开启。
- 区分已配对与近期在线手机，显示最后同步请求时间和电脑额度是否更新。

### 同步与兼容

- 内置手机同步组件，无需另装 Python 或打开独立终端程序。分发包适用于 Windows 10/11 x64。
- 复用 Android 的 HTTPS + 证书指纹配对协议，直接共享桌面版已有额度缓存，避免重复查询在线额度。
- 默认配对目录的证书与设备继续使用；自定义测试目录不自动迁移，可重新配对。Android 0.2.0 支持验证电脑身份后切换无线地址。
- 仅展示当前连接的局域网网卡，优先 Wi-Fi，排除常见 VPN／虚拟网卡和 USB 环回测试地址。
- 提供需管理员授权的局域网放行按钮，规则限定程序、网卡、本机地址、端口和局域网来源。
- 保留 v2.1.2 的手绘动画、完整叶片、自然表情和舒展动作。

### 验证

- 检查 HTTPS 配对、一次性邀请及过期、续期、撤销、旧设备持久化、额度白名单、慢 TLS 连接和父进程退出清理。
- 实测设置页按钮、二维码原图载入、同步关闭、配置持久化和私有配对文件清理。
- EXE 启动、桌面走动、缩团、展开及托盘退出仍使用运行测试检查。


## v2.1.2 — 2026-09-11

- 修复伸懒腰末段叶片被截断：切图按实际留白分隔，并按照整段动作的最大姿势预留边距。
- 补画更加舒展的抬臂和侧伸姿势，双手抬过头顶，随后放松收回；剔除一张叶片方向不一致的侧倾姿势，改为延续上一张顶点姿势。
- 新增“伸展顶点停留（时长占比 %）”设置，默认 18%，可在 0–40% 调整，整段时长仍由伸懒腰时长控制。
- 改善表情贴图感：保留原帧面板的轮廓、底色和纹理，仅替换眼睛、嘴、腮红与泪滴；五官等比贴合，避免拉宽或拉扁。
- 保持 440×420 内部渲染与全部动作的额度表情；新增不等高分格、画布边距、面板纹理保留及顶点停留检查。
- 首次启动按版本顺序迁移已有设置，优先读取 v2.1.1。

## v2.1.1 — 2026-09-11

- 修复躺下时面部深色区域与身体轮廓相连、导致换表情误改叶片和身体边缘的问题，消除这一姿势的断线和缺口。
- 所有动作的内部渲染精度从 220×210 提升至 440×420，显示尺寸保持不变；缩放采用高质量采样，改善轮廓和五官的平滑度。
- 保留原始手绘素材、姿势和动作帧，没有重新设计角色。
- 增加脸部区域占比回归检查，覆盖全部 576 种动作帧／表情组合；限制高清合成缓存，避免内存随表情切换持续增长。
- 升级到 v2.1.1 时迁移 v2.1.0 设置，找不到时回退读取 v2.0.0。

## v2.1.0 — 2026-09-11

### 手绘动作

- 保持最初的扁平手绘角色造型，使用完整图片帧播放。
- 挥手、伸懒腰由各 8 帧增加到各 16 帧，补充抬手、伸展和收回的中间姿势；动作时间轴播放完整 16 帧。
- 其他动作保留原版 8 帧及进出姿势。身体动作合计 72 帧。

### 额度表情与眨眼

- 走路、坐下、躺下、伸懒腰、挥手和缩团均支持平静、开心、担忧、低落四种额度表情。
- 采用新画的动作表情图稿，按每帧脸部方向贴合到原有面板；身体、手脚和叶片不拆分变形。
- 眨眼可在各动作中独立触发；睡下后保持当前情绪的闭眼版本。缩团不再固定笑脸。
- 修复白底处理误伤角色内部浅色区域的问题，减少换表情时的透明缺口。

### 升级

- 下载 `Codex-Usage-Pet-v2.1.0.exe`，先从系统托盘退出旧版，再启动新版。
- 新版使用 `%LOCALAPPDATA%\CodexUsagePet\2.1.0`，首次启动迁移 v2.0.0 的位置和偏好设置。
- 五小时／一周用量、设置页面、托盘退出、默认 60–120 秒随机动作间隔继续保留。

### 验证与说明

- 检查 576 种动作帧／情绪／睁闭眼组合，涵盖透明度、身体轮廓和表情区分；检查完整 16 帧时间轴。
- 新素材为 imagegen 生成的手绘风格图稿；逐帧仍可能有轻微线条变化。增加帧数仅涉及挥手和伸懒腰，其他动作本次更新表情。
- 本次为 Windows 版本更新；Android 预览版没有同步替换素材。

## v2.0.0

- 引入扁平手绘风格角色：七张动作图表、56 帧。
- 支持走路、坐下、躺下、挥手、伸懒腰、缩团和待机额度表情。
