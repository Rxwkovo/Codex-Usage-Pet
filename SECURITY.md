# 安全策略

## 支持范围

当前维护桌面 v2.2.x 与 Android 0.2.x 预览版。安全修复会发布为新的 GitHub Release，不回填旧安装包。

## 报告漏洞

请使用本仓库 GitHub 页面中的 **Security → Report a vulnerability** 私下报告。请提供受影响版本、复现步骤、影响和可行的最小验证样例；请勿在公开 Issue 中发布尚未修复的利用细节、配对码、设备令牌或证书私钥。

## 安全边界

- 手机同步端只监听用户选择的私有局域网 IPv4；本机回环仅用于明确的 USB 测试。
- 网络 API 只有一次性配对和只读额度两个入口，不提供远程控制或命令执行。
- 手机只接收五小时与一周额度、重置时间、更新时间和状态；电脑登录信息不进入同步协议。
- 配对使用短期一次性邀请，后续使用随机设备令牌；电脑只持久化令牌哈希，Android 使用 Keystore 加密配对信息并固定证书指纹。
- 服务限制连接时长、总并发、每来源并发、请求频率、请求头大小和状态表容量。

家庭局域网、Windows 用户账户与用户主动导入的配对码属于信任边界。电脑或手机已被本地恶意软件完全控制时，应用自身无法保证其中的数据安全。
